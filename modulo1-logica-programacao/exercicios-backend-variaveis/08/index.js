let comprimentoCirc, areaCirc, raioCirc;
const pi = Math.PI;

raioCirc = 1;

comprimentoCirc = 2 * pi * Math.pow(raioCirc, 2);
areaCirc = comprimentoCirc / 2;

console.log(`Comprimento do círculo = ${comprimentoCirc}`);
console.log(`Área do círculo = ${areaCirc}`);